def display_board(board)
  cell = " "
  seperator = "-----------"
  bracket = "|"
  
  row_1 = cell + bracket + cell + bracket + cell
  row_2 = cell + bracket + cell + bracket + cell
  row_3 = cell + bracket + cell + bracket + cell
  
  board 
end

def position_taken?(board, index)
end

def valid_move?(board, index)
end

def input_to_index(input)
end

def move(board,token = "X")
end

def turn
end